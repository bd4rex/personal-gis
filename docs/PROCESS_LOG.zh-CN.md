# 实施过程日志

> [English](PROCESS_LOG.md) | 简体中文 · 快照 `2026-08-03T23:12:23+08:00`

> 这是历史实施记录。早期章节中的命令、端口、区域范围和文件名代表当时状态；当前操作以根 README、运维和重建文档为准。

本日志记录 GIS_P 江苏/安徽 MVP 从文件型验证发展到可恢复离线平台的过程。

## 2026-07-03：建立地图流水线

### 目标与工作区

- 明确目标不是小型 MapLibre 演示，而是个人可控、使用开放数据、断网可运行并能扩展全球的系统。
- 首批范围选择江苏和安徽，用较小区域验证完整链路。
- 活动项目从 `C:\Users\Administrator\Documents\个人GIS` 迁至 `D:\GISS`，按约 100GB 初始预算建立 `raw`、`products`、`runtime`、`services`、`scripts`、`web` 和 `docs`。

### OSM、Planetiler 与 PMTiles

- 从 OSM France 下载江苏、安徽 PBF 与状态；发现江苏文件被截断后重新下载校验。
- Planetiler 准备 Natural Earth、水面和湖泊中心线等共享资源。
- 通过显式 bounds 和 `--osm_lazy_reads=false` 解决 Windows/Docker 文件访问问题。
- 生成首批江苏与安徽 PMTiles，证明本地 OSM 到单文件矢量地图链路可行。

### 浏览器与服务

- 初始手写 MapLibre 样式完成验证，但视觉质量有限。
- Python 静态服务改为 nginx，以支持 PMTiles 必需的 `206 Partial Content`。
- 引入 PostGIS、Martin 与个人 `app.places`/`places_web`。
- 试用 OpenFreeMap Liberty 样式、本地 sprites 与 Noto Sans；因两个地图源重复大量图层导致变慢，随后改为更轻量的 OSM 风格默认样式。

## 2026-07-11：可靠性、安全与个人数据

### 安全边界

- 只开放 `127.0.0.1:8080`，不再暴露 PostGIS、Martin 和 FastAPI。
- 镜像固定 digest，密码移入忽略的 `.env`，nginx 只挂载需要目录。
- Martin 只允许 `places_web` 和 `tracks_web`，加入安全首部、dotfile 拒绝和上传大小限制。

### 数据模型与 API

- 一次性初始化 SQL 改为有序迁移。
- 增加点位、MultiLineString 轨迹、媒体、乐观版本、时间、变更日志、GiST、JSONB 与 trigram 索引。
- FastAPI 成为点位/轨迹 CRUD、GPX、图片、状态和 GeoJSON 的写入边界。

### 原子构建与界面

- 下载使用暂存文件和 Osmium 校验；从同一中国快照提取区域，避免边界对象版本冲突。
- Planetiler 产品通过首部、大小和 SHA256 后才替换，最大生成缩放提升到 z16。
- 地图界面增加个人数据、图层、图例、标准/探索样式、区域视图、编辑、GPX、测量、媒体、导出、状态和响应式控件。

### 恢复与测试

- 增加 PostgreSQL dump、媒体、SHA256、路径限制与保留策略。
- 健康检查覆盖 API/PostGIS/Martin 和 PMTiles Range；API 冒烟覆盖 CRUD、搜索、几何、GPX 和媒体清理。
- 容器化 Playwright 验证真实画布、署名、对话框、主题和窄屏，并保存本地截图证据。

## 2026-07-16：搜索、探索、恢复与高级离线能力

### 参考搜索与集合

- 新增 `reference_places` 与 `dataset_state`，从共享 PBF 构建 126,340 个命名 OSM 节点。
- 统一搜索合并个人点位、轨迹与参考地点，参考结果可以复制为个人点位但不修改派生索引。
- 渲染的 POI、地名、道路、水体、山峰、公园和建筑可打开详情并使用附近搜索。
- 加入 Favorites、Travel Plans、Field Research 和自定义集合，个人详情统一展示集合、标签、备注、附近轨迹和照片。

### 完整断网恢复包

- 从个人数据库备份扩展为包含应用、文档、地图、PBF、边界、Planetiler 缓存和 Docker 镜像的完整包。
- 每个文件记录大小和 SHA256；恢复要求空目录，离线加载镜像，恢复数据并执行健康检查。
- 隔离演练在 Docker `--internal` 网络中恢复临时卷，验证记录、搜索、导出、媒体、Martin、nginx、PMTiles 和无外网，并留下 JSON 审计。

### 区域中心与高级引擎

- 单地图目录发展成有版本的区域包注册表，构建过程加入边界、提取、合并、去重、引用检查、暂存和来源清单。
- 苏皖与沪浙组合包用于验证多区域、切换与恢复包泛化，之后被独立省份模型替代。
- Nominatim 提供地址与反查；Valhalla 生成路线与海拔；58 个 HGT 支持路线、点高程和地形。
- 地形瓦片首次生成从约 60 秒优化到约 2.1 秒，缓存后约 0.23 秒。
- Kiwix 提供校验过的中文维基百科；图层页增加地形和应急，路线面板支持模式、端点、中文指令、时间距离、海拔、保存和清除。

## 2026-07-17：省级目录与全球资源

### 34 个省级单元

- 原有七个组合包被降为兼容过渡，schema-v3 目录定义 34 个独立省级单元、六个展示分组、边界、来源、估算和生命周期命令。
- 大陆从版本化中国快照提取，台湾使用独立 Geofabrik 来源。
- 首次校准：江苏约 83.2MiB PBF/412.7MiB PMTiles，安徽约 43.1MiB/329.5MiB，当前机器单省完整构建约 17 分钟。
- 上海、浙江也完成独立迁移；随后删除苏皖/沪浙组合包及旧回退，释放约 2.16GiB。
- 共享索引按四省重新生成到 340,333 个参考地点，随后历史范围在 2026-08-03 再收缩到当前江苏/安徽。

### 高级恢复验证

- `20260716-230426` 恢复包包含 960 个文件、14.64GiB 校验数据。
- 隔离演练验证参考地点、集合、迁移、PMTiles、Martin、Nominatim、Valhalla、海拔、Kiwix、nginx、导出和外网阻断。
- 修复 Windows PowerShell 下 Docker 卷发现、Kiwix OPDS 验证、Valhalla HGT 只读挂载和 Nominatim 冷查询重试问题。

### 全球资源实现

- 生成 547 个浏览节点和 554 个可构建 Geofabrik 包。
- 安装 Natural Earth 概览、29 城市七天天气、5,718 个航海要素、中文维基导游和路线 TTS。
- 所有展示资源都接入真实安装/更新、健康、测试和恢复包，不保留 UI 占位项。
- Monaco 只用于验证全球直接来源的构建/删除闭环，验证后移除。

### 维护执行模型

- 资源页面改为持久左右分栏，保留全球层级与当前选择。
- `POST /api/maintenance/jobs` 只接受目录 ID 与固定资源白名单。
- 单实例 Windows 工作器记录心跳、原子 JSON、队列、运行、成功/失败和日志。
- 天气与目录可自动更新；地图、共享索引和大文件保持显式操作。

## 2026-07-22：可靠性与所有权

- 更新判断从日期猜测改为可信 Geofabrik/OSM 复制状态。
- 维护队列增加优先级、取消、重试/退避、成功后调度和有界历史。
- 加入轨迹版本编辑、轨迹照片所有权、级联/孤儿清理、单条/全部 GPX 和带清单个人 ZIP。
- 安装每日 03:00 校验备份，支持第二物理盘镜像，并把恢复包纳入存储统计。
- 修复全球目录编码、删除 Monaco、拆分 API 帮助模块并扩展版本冲突、路线、媒体、GPX 与恢复导出测试。
- 所有已安装省份同时渲染；区域快捷方式只控制相机。
- 任务进度回到资源行内，解析 curl 字节速度与 Planetiler 瓦片/要素速度，不制造不可测量进度。
- 资源盘点改为持久缓存与后台线程：活动构建下首次约 15.9 秒、缓存约 13 毫秒，替代约 57 秒首屏。
- 研究 OsmAnd 的资源分类和缓存优先原则，但不复制其 UI/UX 资产或代码。

## 2026-07-31：更新闭环与全球所有权提示

- 更新流程统一刷新上游状态与来源，使用暂存中国 PBF、Osmium 校验、原子激活，并让同序列省份复用快照。
- API 拒绝当前序列的重复更新，“重建”独立存在。
- 天气、航海、概览、百科和旅行指南缺失时显示真实安装动作；库、样式、组件和缓存不再伪装为图层。
- 加入低缩放本地全球地图，定位目录包后选择临时在线查看或精确离线构建。
- 恢复包使用已校验清单计量，避免递归扫描大型 payload；健康检查优先使用最后完整盘点。
- Playwright 增加目录搜索、全球定位、选择保持、在线切换和返回同一构建目标。

## 2026-08-02：存储迁移、来源控制与 GIS_P 品牌

### D 盘迁移与维护连续性

- Docker WSL 数据迁移到 `D:\DockerData\wsl`，验证容器、镜像、卷、个人计数、备份、健康和 VHD 写入后才删除 C 盘旧 VHD。
- 项目统一到 `D:\GISS`，C 盘原路径改为 Junction。
- PMTiles 大型校验改为 `202 Accepted` 后台任务；资源盘点改为 stale-while-refresh。
- 修复冒烟测试只比较中国省份的问题，使共享范围包含所有启用全球包。
- 诊断 Nominatim 3,370 万 `placex` 已存在但后处理与 GIN 索引未完成的问题，续建 `db-postprocess` 后通过完整数据库自检。

### 地图来源与覆盖

- WiFi 二元开关改成“仅离线、OSM Standard、OpenFreeMap”来源菜单，显示首选、实际、回退、连接与本地覆盖。
- 覆盖中心改为侧栏之间的可见地图中心，加入 bounds 预筛选，防止错误边界匹配远处视口。
- 国家提示使用中文名称，并统一“下载离线地图”用语。

### 品牌与默认工作区

- 用户界面、API 元数据、GPX、命令输出、测试和主文档统一为 GIS_P。
- `D:\GISS`、`giss-*`、环境变量、浏览器键和计划任务继续作为兼容标识。
- 默认首屏收起侧栏并同步动画、焦点、ARIA、按钮和覆盖判断。
- 共享索引区分“覆盖完整”与“范围完全最新”，多余旧范围不阻止服务但要求清理重建。

## 2026-08-03：当前范围、OSM Carto 与存储所有权

- 从当前江苏/安徽两个已安装包重建 Nominatim 与 Valhalla，候选验收后原子切换；新恢复包验证通过后移除旧德国范围。
- 增加候选后期续建、活动挂载感知清理、动态验证点、紧凑 Valhalla 保留和活动指针启动检测。
- 恢复 schema v4 加入 Nominatim 与 OSM Carto 快照、OSM Carto 来源记录，并默认只保留一个有效完整包。
- 移除德国、Monaco、上海、浙江和重复省级源；旧下载入口只维护共享中国快照。
- 引入 OSM Carto 默认地图、可续建外部数据、独立数据库与缓存；PMTiles 保留为交互矢量来源，并增加高细节叠加构建。
- 删除未挂载旧 PostGIS 卷、无用镜像和可再生构建缓存，执行 trim 与离线压缩，把 Docker VHDX 从 140.81GiB 降至 24.51GiB。
- 重启全部八个容器并通过健康与完整功能测试。

## 关键经验

- MapLibre 是渲染器，不是完整地图产品。
- 地图体验由样式、图例、控件、信息密度和 I/O 共同决定。
- 多源重复完整专业样式会降低性能；默认浏览应优先熟悉度与速度。
- PMTiles + nginx 是轻量可移植路径，OSM Carto 提供熟悉视觉，二者可以并存。
- PostGIS + FastAPI + Martin 适合作为个人数据边界。
- GitHub 应保存可重现系统，而不是大型派生地图或私人资料。
